{
  "name": "whisper-guard",
  "version": "1.0.0",
  "private": true,
  "description": "Whisper Guard browser extension: proactive phishing and brand protection, on-device first, powered by the Whisper security graph when you sign in.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "icons": "node scripts/gen-icons.mjs",
    "psl": "node scripts/fetch-psl.mjs",
    "build": "npm run typecheck && node scripts/build.mjs chromium && node scripts/build.mjs firefox",
    "build:chromium": "npm run typecheck && node scripts/build.mjs chromium",
    "build:firefox": "npm run typecheck && node scripts/build.mjs firefox",
    "package": "npm run build && node scripts/package.mjs",
    "e2e": "playwright test e2e/mocked.spec.ts e2e/shield.spec.ts",
    "e2e:live": "playwright test e2e/live.spec.ts",
    "e2e:all": "playwright test",
    "e2e:firefox": "node scripts/e2e-firefox.mjs"
  },
  "license": "MIT",
  "author": "Whisper Security (viaGraph B.V.)",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.4.5",
    "web-ext": "^10.5.0"
  }
}
